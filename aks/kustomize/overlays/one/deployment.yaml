apiVersion: apps/v1
kind: Deployment
metadata:
  name: vbserver-deployment
spec:
  template:
    spec:
      containers:
      - env:
        - name: DB_URL
          value: "//my/server"
        name: vbserver-container
          